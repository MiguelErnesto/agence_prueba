<?php

use App\Models\main;
use App\Models\front_preview;
use App\Models\navbar;
use Illuminate\Support\Facades\Schema;

/* if (Schema::hasTable('mains')) {
    $main = main::all();
    config(['app.nombre_principal' => $main[0]->name]);

    $front_preview = front_preview::all();
    config(['app.front_url' => $front_preview[0]->url]);

    $navbar = navbar::all();
    config(['app.nav_section1' => $navbar[0]->item1]);
    config(['app.nav_section2' => $navbar[0]->item2]);
    config(['app.nav_section3' => $navbar[0]->item3]);
    config(['app.nav_section4' => $navbar[0]->item4]);
    config(['app.nav_section5' => $navbar[0]->item5]);
    config(['app.nav_section6' => $navbar[0]->item6]);

    config(['app.nav_chk1' => $navbar[0]->chk1]);
    config(['app.nav_chk2' => $navbar[0]->chk2]);
    config(['app.nav_chk3' => $navbar[0]->chk3]);
    config(['app.nav_chk4' => $navbar[0]->chk4]);
    config(['app.nav_chk5' => $navbar[0]->chk5]);
    config(['app.nav_chk6' => $navbar[0]->chk6]);
} */
